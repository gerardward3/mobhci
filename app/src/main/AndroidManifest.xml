<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teamnaji.mhci">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <meta-data
        android:name="com.google.android.gms.vision.DEPENDENCIES"
        android:value="barcode" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ManualCodeEnter"
            android:label="@string/title_activity_manual_code_enter" />
        <activity
            android:name=".Money"
            android:label="@string/title_activity_money"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Fitness"
            android:label="@string/title_activity_fitness"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UnlockBike" />
        <activity android:name=".QrCodeScanner" />
        <activity android:name=".CodeAcceptedScreen"></activity>
    </application>

</manifest>